These tests need FastAPI/SQLAlchemy/bcrypt/pytest (`pip install -r backend/requirements-dev.txt`) and are run with
`pytest backend/tests ml/tests`.  They were written alongside the code but could NOT be executed in the authoring
sandbox (package index blocked) - run them first and fix any environment-specific findings.
